using namespace cv;
using namespace std;

//Variables
enum SignDetected {SIGN_NONE=0,SIGN_LEFT,SIGN_RIGHT, SIGN_STRAIGHT, SIGN_STOP, SIGN_U_TURN, SIGN_UNKNOWN};

//Functions
int findSign(InputArray src, OutputArray dst, bool display, double dWidth, double dHeight);

//Code
int findSign(InputArray src, OutputArray dst, bool display, double dWidth, double dHeight){
	return 0;
}